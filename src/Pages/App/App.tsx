import * as React from "react";
import { Header, OverviewContainer } from "./../../Components";

export const App = () => {
  return (
    <React.Fragment>
      <Header />
      <OverviewContainer />
    </React.Fragment>
  );
};
